{
  "name": "markdown-threads",
  "displayName": "Markdown Threads",
  "description": "Inline feedback and comments on markdown files stored in Git",
  "version": "1.0.7",
  "publisher": "busa-dave-13",
  "license": "MIT",
  "icon": "media/markdown-threads-icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/busadave13/markdown-threads"
  },
  "keywords": [
    "markdown",
    "review",
    "comments",
    "feedback"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:markdown",
    "onCommand:markdownThreads.openPreview",
    "onView:markdownThreads.files"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "markdownThreads",
          "title": "Markdown Threads",
          "icon": "media/sidebar-icon.svg"
        }
      ]
    },
    "views": {
      "markdownThreads": [
        {
          "id": "markdownThreads.files",
          "name": "Markdown Files"
        }
      ]
    },
    "commands": [
      {
        "command": "markdownThreads.openPreview",
        "title": "Markdown: Review and Comment",
        "category": "Markdown Threads",
        "icon": "$(open-preview)"
      },
      {
        "command": "markdownThreads.refreshFiles",
        "title": "Refresh",
        "category": "Markdown Threads",
        "icon": "$(refresh)"
      },
      {
        "command": "markdownThreads.selectFolder",
        "title": "Filter by Folder",
        "category": "Markdown Threads",
        "icon": "$(folder)"
      }
    ],
    "configuration": {
      "title": "Markdown Threads",
      "properties": {
        "markdownThreads.excludeFolders": {
          "type": "array",
          "scope": "resource",
          "items": {
            "type": "string"
          },
          "default": [
            "node_modules",
            ".github",
            ".git",
            "out",
            "dist",
            "bin",
            "obj",
            ".vscode",
            ".vscode-test"
          ],
          "description": "Folders to exclude from the Markdown Files tree view."
        }
      }
    },
    "menus": {
      "editor/title": [
        {
          "command": "markdownThreads.openPreview",
          "when": "resourceLangId == markdown",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "markdownThreads.openPreview",
          "when": "resourceExtname == .md",
          "group": "navigation"
        }
      ],
      "view/title": [
        {
          "command": "markdownThreads.selectFolder",
          "when": "view == markdownThreads.files",
          "group": "navigation@1"
        },
        {
          "command": "markdownThreads.refreshFiles",
          "when": "view == markdownThreads.files",
          "group": "navigation@2"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run esbuild-production",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "esbuild": "node esbuild.config.mjs",
    "esbuild-production": "node esbuild.config.mjs --production",
    "esbuild-watch": "node esbuild.config.mjs --watch",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint . --ext ts",
    "test": "node ./out/test/runTest.js",
    "package": "npm run esbuild-production && vsce package"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.10.0",
    "@types/uuid": "^9.0.7",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "@vscode/test-electron": "^2.3.8",
    "@vscode/vsce": "^2.22.0",
    "esbuild": "^0.28.0",
    "eslint": "^8.55.0",
    "glob": "^10.3.10",
    "mocha": "^10.2.0",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "markdown-it": "^14.1.1",
    "simple-git": "^3.21.0",
    "uuid": "^9.0.1"
  }
}
